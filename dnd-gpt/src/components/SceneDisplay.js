import React from 'react';
import './SceneDisplay.css'; // Create this CSS file for styling

const SceneDisplay = ({ scene }) => {
  return (
    <div className="scene-display">
      {/* Scene image and description will go here */}
    </div>
  );
};

export default SceneDisplay;
